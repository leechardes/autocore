# Config App Backend Configuration

# Server
CONFIG_APP_PORT=8081
CONFIG_APP_HOST=0.0.0.0
ENV=production

# MQTT
MQTT_BROKER=localhost
MQTT_PORT=1883
MQTT_USERNAME=autocore
MQTT_PASSWORD=SUA_SENHA_AQUI

# Database
DATABASE_PATH=../../database/autocore.db

# Security
# IMPORTANTE: Estas chaves serão geradas automaticamente no deploy
# Para gerar manualmente: openssl rand -hex 32
SECRET_KEY=SERA_GERADA_AUTOMATICAMENTE_NO_DEPLOY
JWT_SECRET_KEY=SERA_GERADA_AUTOMATICAMENTE_NO_DEPLOY
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRES=3600

# Logging
LOG_LEVEL=INFO
LOG_FILE=../../logs/config-app.log

# CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:5000
EOF < /dev/null
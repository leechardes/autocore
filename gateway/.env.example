# Gateway Configuration

# MQTT
MQTT_BROKER=localhost
MQTT_PORT=1883
MQTT_USERNAME=autocore
MQTT_PASSWORD=SUA_SENHA_AQUI

# Gateway
GATEWAY_ID=local-gateway
GATEWAY_NAME=AutoCore Gateway

# Database
DATABASE_PATH=../database/autocore.db

# Security
# IMPORTANTE: Esta chave será gerada automaticamente no deploy
# Para gerar manualmente: openssl rand -hex 32
SECRET_KEY=SERA_GERADA_AUTOMATICAMENTE_NO_DEPLOY
ENABLE_AUTH=true

# Logging
LOG_LEVEL=INFO
LOG_FILE=../logs/gateway.log
EOF < /dev/null
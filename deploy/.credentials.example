# Exemplo de arquivo de credenciais para AutoCore
# 
# IMPORTANTE: 
# 1. Copie este arquivo para .credentials
# 2. Preencha com suas credenciais reais
# 3. NUNCA commite o arquivo .credentials no git
# 4. O arquivo .credentials já está no .gitignore
#
# Uso:
#   cp .credentials.example .credentials
#   chmod 600 .credentials
#   vim .credentials  # editar com suas credenciais
#

# ============================================
# RASPBERRY PI - SSH
# ============================================

# Usuário principal do sistema AutoCore
RASPBERRY_USER=autocore
RASPBERRY_PASS=SUA_SENHA_AQUI

# IP ou hostname do Raspberry Pi
RASPBERRY_IP=10.0.10.119
# ou
# RASPBERRY_HOSTNAME=autocore.local

# ============================================
# MQTT BROKER
# ============================================

MQTT_USER=autocore
MQTT_PASS=SUA_SENHA_MQTT_AQUI
MQTT_HOST=localhost
MQTT_PORT=1883

# ============================================
# DATABASE
# ============================================

# Se usar PostgreSQL no futuro
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=autocore
# DB_USER=autocore
# DB_PASS=SUA_SENHA_DB

# ============================================
# API KEYS (se necessário)
# ============================================

# Para integrações futuras
# GOOGLE_MAPS_API_KEY=
# WEATHER_API_KEY=
# OPENAI_API_KEY=

# ============================================
# CONFIGURAÇÕES DE REDE
# ============================================

# WiFi do Raspberry Pi (se configurar via script)
# WIFI_SSID=SuaRedeWiFi
# WIFI_PASSWORD=SuaSenhaWiFi

# ============================================
# PORTAS DOS SERVIÇOS
# ============================================

CONFIG_APP_PORT=5000
CONFIG_FRONTEND_PORT=3000
GATEWAY_PORT=8080

# ============================================
# SEGURANÇA
# ============================================

# Gateway Secret Key (gerar com: openssl rand -hex 32)
GATEWAY_SECRET_KEY=SUA_SECRET_KEY_GATEWAY_AQUI

# Config App Secret Keys (gerar com: openssl rand -hex 32)
CONFIG_SECRET_KEY=SUA_SECRET_KEY_CONFIG_AQUI
JWT_SECRET_KEY=SUA_JWT_SECRET_KEY_AQUI

# ============================================
# AMBIENTE
# ============================================

ENVIRONMENT=production
LOG_LEVEL=INFO

# ============================================
# BACKUP
# ============================================

# Configurações de backup automático
# BACKUP_PATH=/opt/autocore/backups
# BACKUP_RETENTION_DAYS=30
# BACKUP_SCHEDULE="0 2 * * *"  # 2AM daily

# ============================================
# NOTAS
# ============================================
#
# Credenciais de referência (REMOVER EM PRODUÇÃO):
# 
# Usuário AutoCore padrão:
#   Username: autocore
#   Password: (definir senha forte)
#
# Recomendações de segurança:
# 1. Use senhas com pelo menos 16 caracteres
# 2. Misture letras, números e símbolos
# 3. Evite palavras do dicionário
# 4. Configure chave SSH ao invés de senha quando possível
# 5. Rotacione senhas regularmente
#
# Exemplo de senha forte:
#   xK9mN3pQ7rL5vB2wA8sD6fG4hJ1yT0uE
#
# Para gerar senha forte:
#   openssl rand -base64 32
#   pwgen -s 32 1
#
# Para configurar chave SSH:
#   ssh-keygen -t rsa -b 4096
#   ssh-copy-id autocore@RASPBERRY_IP